/* eslint-disable no-unused-vars */
const assert = require("assert");
const eversendSdkNode = require("../index.js");

describe("eversendSdkNode", () => {
  it("has a test", () => {
    assert(true, "eversendSdkNode should have a test");
  });
});
